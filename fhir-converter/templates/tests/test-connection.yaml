apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fhir-converter.fullname" . }}-test-connection"
  labels:
    {{- include "fhir-converter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fhir-converter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
