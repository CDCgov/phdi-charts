apiVersion: v1
kind: Service
metadata:
  name: alerts-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    app: alerts-app