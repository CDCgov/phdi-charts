apiVersion: v1
kind: Pod
metadata:
  name: '{{ .Release.Name}}-fhir-converter-service'
  labels:
    app: fhir-converter-app
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name}}-fhir-converter-service:{{ .Values.service.port }}']
  restartPolicy: Never
