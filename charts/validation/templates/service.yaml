apiVersion: v1
kind: Service
metadata:
  name: validation-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    app: validation-app