apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "message-parser.fullname" . }}-test-connection"
  labels:
    {{- include "message-parser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "message-parser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
