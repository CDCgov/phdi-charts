apiVersion: v1
kind: Pod
metadata:
  name: '{{ .Release.Name}}-ingestion-service'
  labels:
    app: ingestion-app
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name}}-ingestion-service:{{ .Values.service.port }}']
  restartPolicy: Never
