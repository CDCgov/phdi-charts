apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-parser.fullname" . }}
  labels:
    {{- include "message-parser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-parser.selectorLabels" . | nindent 4 }}
