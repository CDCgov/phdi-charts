apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhir-converter.fullname" . }}
  labels:
    {{- include "fhir-converter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fhir-converter.selectorLabels" . | nindent 4 }}
