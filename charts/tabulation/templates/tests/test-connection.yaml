apiVersion: v1
kind: Pod
metadata:
  name: '{{ .Release.Name}}-tabulation-service'
  labels:
    app: tabulation-app
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name}}-tabulation-service:{{ .Values.service.port }}']
  restartPolicy: Never
