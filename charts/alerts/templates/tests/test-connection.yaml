apiVersion: v1
kind: Pod
metadata:
  name: '{{ .Release.Name}}-alerts-service'
  labels:
    app: alerts-app
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name}}-alerts-service:{{ .Values.service.port }}']
  restartPolicy: Never
